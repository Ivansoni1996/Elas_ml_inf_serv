# Elas_ml_inf_serv
The Elastic ML Inference Serving project focuses on the practical application of cloud computing principles, specifically elasticity and resource orchestration. The objective is to build an automated scaling system within a Kubernetes (K8s) cluster to serve a machine learning image classification model (ResNet18).
